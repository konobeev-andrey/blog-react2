import React from "react";

const NotFound = ({text}) => {
    return (<div style={{
        color: "#24324F",
        fontWeight: "bold",
        textAlign:"center",
        marginLeft: "auto",
        marginRight: "auto"}}>
       <p>{text}</p>
    </div>)

}

export default NotFound;